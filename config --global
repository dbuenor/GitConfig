[user]
        email = dbueno@evicertia.com
        name = David Bueno
[core]
        autocrlf = false
[alias]
        merge-diff = !bash -c 'git diff $(git merge-base $1 $2) $2 ${@: 3} ' -
        st = status
        mt = mergetool
        di = diff
        rc = rebase --continue
        ra = rebase --abort
        undo = checkout --
        up = remote update --prune
        ch = checkout
        vim = vim -b
        re = reset
        rh = reset --hard
        cl = clean -f
        rmb = push origin --delete
        bm = branch --merged origin/master -a
[merge]
        tool = vsdiffmerge
[mergetool "p4merge"]
        path = D:\\Software\\P4Merge\\p4merge.exe
[mergetool "meld"]
        path = D:\\Software\\Meld\\Meld.exe
[mergetool "vsdiffmerge"]
        cmd = \"vsdiffmerge.exe\" \"$REMOTE\" \"$LOCAL\" \"$BASE\" \"$MERGED\" //m
        keepBackup = false
[color "diff"]
        whitespace = red reverse
[color]
        diff = auto
[push]
        default = simple
